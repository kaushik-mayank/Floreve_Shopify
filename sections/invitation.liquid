{%- liquid
  assign image = section.settings.image
  assign has_media = false
  if image != blank or section.settings.image_asset != blank or section.settings.video_asset != blank
    assign has_media = true
  endif
  assign scheme = section.settings.color_scheme
  if has_media
    assign scheme = 'dark'
  endif
-%}

<section
  class="hero hero--{{ section.settings.height }} hero--center scheme scheme--{{ scheme }}"
  style="--scrim: {{ section.settings.overlay_opacity | divided_by: 100.0 }}; --focal: {{ section.settings.focal_x }}% {{ section.settings.focal_y }}%;"
>
  {%- if has_media -%}
    <div class="hero__media">
      {% render 'section-media',
        image: image,
        image_asset: section.settings.image_asset,
        video_asset: section.settings.video_asset,
        poster_asset: section.settings.poster_asset,
        alt: section.settings.image_alt,
        focal: section.settings.focal
      %}
    </div>
    <div class="hero__scrim" aria-hidden="true"></div>
  {%- endif -%}

  <div class="hero__inner page-width">
    <div class="hero__content hero__content--center reveal">
      {%- if section.settings.show_mark -%}
        {% render 'calyx', size: '2.5rem' %}
      {%- endif -%}

      {%- if section.settings.eyebrow != blank -%}
        <span class="eyebrow">{{ section.settings.eyebrow }}</span>
      {%- endif -%}

      {%- if section.settings.heading != blank -%}
        <h2 class="h1 hero__title">{{ section.settings.heading }}</h2>
      {%- endif -%}

      {%- if section.settings.text != blank -%}
        <p class="lede hero__text">{{ section.settings.text }}</p>
      {%- endif -%}

      <div class="hero__actions">
        {%- if section.settings.primary_label != blank -%}
          <a
            href="{{ section.settings.primary_url | default: routes.all_products_collection_url }}"
            class="btn btn--primary"
          >
            <span class="btn__label">{{ section.settings.primary_label }}</span>
          </a>
        {%- endif -%}

        {%- if section.settings.secondary_label != blank -%}
          <a href="{{ section.settings.secondary_url }}" class="btn btn--outline">
            <span class="btn__label">{{ section.settings.secondary_label }}</span>
          </a>
        {%- endif -%}
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Invitation",
  "tag": "div",
  "settings": [
    {
      "type": "paragraph",
      "content": "The closing note of a page — an open door rather than a hard sell."
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Background image"
    },
    {
      "type": "text",
      "id": "image_asset",
      "label": "or an image filename in assets/",
      "info": "For example homepage-lifestyle-01.jpg. Ignored when an image is chosen above."
    },
    {
      "type": "text",
      "id": "video_asset",
      "label": "or a video filename in assets/",
      "info": "For example hero-video.mp4. Plays silently on loop and takes priority over the image."
    },
    {
      "type": "text",
      "id": "poster_asset",
      "label": "Video poster filename",
      "info": "Shown while the video loads. For example hero-poster.jpg."
    },
    {
      "type": "text",
      "id": "image_alt",
      "label": "Describe the image",
      "info": "Read aloud by screen readers when the image comes from assets/."
    },
    {
      "type": "select",
      "id": "color_scheme",
      "label": "Colour scheme when there is no image",
      "options": [
        { "value": "parchment", "label": "Parchment" },
        { "value": "alt", "label": "Warm parchment" },
        { "value": "dark", "label": "Atelier black" },
        { "value": "sage", "label": "Botanical sage" },
        { "value": "blush", "label": "Pressed blush" }
      ],
      "default": "alt"
    },
    {
      "type": "select",
      "id": "height",
      "label": "Height",
      "options": [
        { "value": "sm", "label": "Compact" },
        { "value": "md", "label": "Medium" },
        { "value": "lg", "label": "Tall" }
      ],
      "default": "md"
    },
    {
      "type": "range",
      "id": "overlay_opacity",
      "min": 0,
      "max": 80,
      "step": 5,
      "unit": "%",
      "label": "Shade behind the text",
      "default": 45
    },
    {
      "type": "text",
      "id": "focal",
      "label": "Focal point",
      "default": "50% 50%",
      "info": "Which part of the image stays in frame when it is cropped."
    },
    {
      "type": "range",
      "id": "focal_x",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "%",
      "label": "Focal point — horizontal",
      "default": 50
    },
    {
      "type": "range",
      "id": "focal_y",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "%",
      "label": "Focal point — vertical",
      "default": 50
    },
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "checkbox",
      "id": "show_mark",
      "label": "Show the calyx mark",
      "default": true
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": "The collection"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Find the one that speaks for you"
    },
    {
      "type": "textarea",
      "id": "text",
      "label": "Text",
      "default": "Each piece is made by hand in small numbers. Take your time — and write to us if you would like to see one in person."
    },
    {
      "type": "text",
      "id": "primary_label",
      "label": "First button",
      "default": "Explore the collection"
    },
    {
      "type": "url",
      "id": "primary_url",
      "label": "First link"
    },
    {
      "type": "text",
      "id": "secondary_label",
      "label": "Second button",
      "default": "Contact the atelier"
    },
    {
      "type": "url",
      "id": "secondary_url",
      "label": "Second link"
    }
  ],
  "presets": [{ "name": "Invitation" }]
}
{% endschema %}
